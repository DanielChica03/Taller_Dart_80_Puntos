void main() {
  double cap = 750000;
  double inte = cap * 0.02;
  double total = cap + inte;
  print(
    "El interes ganado fue de: ${inte} y el total final del interes mas el capital es: ${total}",
  );
}
